{
  "name": "cycle-website",
  "version": "1.0.0",
  "description": "Presentational web-page for Mobile App",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "dev": "rimraf ./development && rimraf ./.cache && parcel public/*.html -p 4200",
    "build": "parcel build public/index.html --out-dir dist --public-url ./"
  },
  "author": "Haris Zenovic",
  "license": "ISC",
  "dependencies": {
    "jquery": "^3.6.0",
    "parcel": "^2.0.1"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.0.1",
    "@parcel/transformer-sass": "^2.0.1",
    "@parcel/transformer-webmanifest": "^2.0.1",
    "rimraf": "^2.7.1",
    "sass": "^1.26.8"
  }
}
