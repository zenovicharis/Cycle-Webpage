{
  "name": "cycle-website",
  "version": "1.0.0",
  "description": "Presentational web-page for Mobile App",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "dev": "rimraf ./development && rimraf ./.cache && parcel public/index.html -p 4200",
    "build": "parcel build public/index.html --out-dir dist --public-url ./"
  },
  "author": "Haris Zenovic",
  "license": "ISC",
  "dependencies": {
    "parcel": "^1.12.4"
  },
  "devDependencies": {
    "rimraf": "^2.7.1",
    "sass": "^1.26.8"
  }
}
